'strict';

// import { ulRegister } from '../../register-index.js';

// export let register;
// const liClass = 'li-register';

// fetch('../components/webe-login/resources/webLog-data/webLog-data.json')
// 	.then(response => response.json())
// 	.then(data => {
// 		if (data.length >= 1) {
// 			appendData(data);
// 		} else {
// 			if (data.length === 0) {
// 				console.log('No data found');
// 			}
// 		}
// 	})
// 	.catch(error => {
// 		console.log(error);
// 	});

// const appendData = data => {
// 	for (let i = 0; i < data.length; i++) {
// 		register.innerHTML = `${[...data[i]]}`;
// 	}

	// async function hydrateList(dataList, attribute, element) {
	// 	await dataList.forEach(item => {
	// 		const li = document.createElement('li');
	// 		li.append(item);
	// 		li.setAttribute('class', attribute);
	// 		element.append(li);
	// 	});
	// }
	// hydrateList(register, liClass, ulRegister);
};
